// TODO: Implement the widget for Occupants feature.
import 'package:flutter/material.dart';

class OccupantsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Occupants Widget'),
    );
  }
}